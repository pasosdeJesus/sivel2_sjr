<%= f.simple_fields_for :desplazamiento, f.object.desplazamiento.order(:id) do |desplazamiento| %>
  <%= render 'desplazamiento_fields', f: desplazamiento %>
<% end %>
<div class="links">
  <%= link_to_add_association 'Añadir Desplazamiento', f, :desplazamiento, {
    class: 'btn btn-sm btn-primary',
    "data-ajax" => File.join(
      Rails.configuration.relative_url_root,
      "desplazamientos/nuevo"
    ), 
    "data-ajaxdata" => "caso_id" }
  %>
</div>
